# CuisineX — projection publique retirée

La projection statique publique de CuisineX a été retirée le 2026-09-24.

La source canonique et le runtime complet vivent dans le dépôt privé `nepheris/nLab`, projet P019 — nLab CuisineX.

La version active cible est la Web App Google Apps Script authentifiée :
- compte Google requis ;
- contrôle `ALLOWED_EMAILS` ;
- Session Guard CuisineX ;
- scopes `read`, `authoring` et `publish`.

Ce dossier public ne doit plus contenir de recettes, manifestes, données Ciqual, inventaires matériels ou autres données runtime CuisineX.
